const greet = (name) => {
  return `Hello, ${name}!`;
};

const bye = (name) => {
  return `Goodbye, ${name}!`;
};

const night = (name) => {
  return `Goodnight, ${name}!`;
};

module.exports = { greet, bye, night };
